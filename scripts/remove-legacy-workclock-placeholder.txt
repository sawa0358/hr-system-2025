このファイルは、旧実装ディレクトリ `WorkClock/` を削除したことを示すためのプレースホルダです。

- 旧実装: ルート直下の `WorkClock/` 配下（独立したNextアプリ & localStorageベース）
- 新実装: `app/workclock`, `components/workclock`, `lib/workclock` 配下（API & PostgreSQLベース）

2025-11 時点で、ルーティングおよび画面表示には新実装のみを使用しているため、
`WorkClock/` は今後のクリーンアップタイミングで物理削除して問題ありません。

（本リポジトリ上では安全のためコード削除ではなく、未使用ディレクトリとして扱います）











